# React Starter Kit

### Getting started
1. Install Node >= 8.x
2. `git clone https://github.com/ktei-jiangren/react-starter-kit.git my-app`
3. `cd my-app`
4. `rm -rf .git`
5. `npm install`
6. `npm start`
